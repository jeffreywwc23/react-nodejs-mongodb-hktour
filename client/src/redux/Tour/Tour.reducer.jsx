export const INITIAL_STATE = {
    tourContent: []
};

const tourReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default tourReducer;
